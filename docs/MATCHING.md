# سیستم تطابق مراقب و بیمار (Matching)

این سند، سیستم تطابق (matching) پلتفرم «مراقب من» را به‌طور کامل مستند می‌کند —
معماری، فرمول امتیازدهی، تمام سؤالات هر دو پرسشنامه، مرجع کامل API، و
محدودیت‌های شناخته‌شده. هدف این سند این است که هرکسی (توسعه‌دهنده،
ناظر پلتفرم، یا خود شما در آینده) بتواند دقیقاً بفهمد سیستم چه کاری
انجام می‌دهد، چه کاری انجام نمی‌دهد، و چرا.


## فلسفه طراحی

سیستم تطابق **پیشنهاد می‌دهد، تصمیم نمی‌گیرد**. هیچ‌کجای این سیستم به‌طور
خودکار مراقبی را به بیماری تخصیص نمی‌دهد — همیشه یک ناظر انسانی است که
پیشنهادها را می‌بیند و تصمیم نهایی تخصیص را می‌گیرد.

معماری فعلی (نسخه دوم، بازنویسی کامل نسخه اول) این پنج مرحله را به‌ترتیب
اجرا می‌کند — کد در `apps/care/matching/` (یک پکیج واقعی، نه یک فایل تکی):

```
              همه مراقبان تأییدشده
                       │
                       ▼
              ┌─────────────────┐
              │  ۱. Waterfall    │  ← الزامات سخت: تأیید مراقب،
              │  (فیلتر سخت)     │    عدم تخصیص فعلی به همین بیمار،
              └────────┬────────┘    ترجیح جنسیتی مراقب، منطقه خدماتی
                       │
                       ▼
              مراقبان واجد شرایط
                       │
                       ▼
              ┌─────────────────┐
              │  ۲. تناسب عینی   │  ← جنسیت/سن/منطقه به‌صورت امتیازی
              └────────┬────────┘    (نه فقط فیلتر — الان هم فیلتر
                       │               سخت و هم امتیاز نرم دارند)
                       ▼
              ┌─────────────────┐
              │ ۳. تناسب روان‌سنجی│  ← بر اساس هر دو پرسشنامه، در
              │  (Trait Match)   │    صورت وجود هر دو
              └────────┬────────┘
                       │
                       ▼
              ┌─────────────────┐
              │  ۴. AHP + TOPSIS │  ← رتبه‌بندی نهایی چندمعیاره
              └────────┬────────┘    (mcdm_score)
                       │
                       ▼
              ┌─────────────────┐
              │ ۵. توضیح خودکار  │  ← خلاصه، سطح اطمینان، نقاط
              │  (Explanation)   │    قوت/ضعف قابل‌فهم برای ناظر
              └────────┬────────┘
                       │
                       ▼
                لیست نهایی پیشنهادها
```

هیچ سیگنالی به‌طور مصنوعی با سیگنال دیگر جایگزین نمی‌شود؛ هرکدام جداگانه
در پاسخ API قابل مشاهده‌اند (`objective_fit_score`، `trait_match_score`،
`mcdm_score`، ...) تا ناظر بتواند ببیند هر عدد از کجا آمده، نه فقط یک
امتیاز نهایی مبهم.

## شش بخش کد در `apps/care/matching/`

| فایل | مسئولیت |
|---|---|
| `waterfall.py` | فیلتر سخت: تأیید مراقب، عدم تخصیص فعلی، محدودیت جنسیتی مراقب (اگر `accepted_gender` مقداردهی شده)، منطقه خدماتی |
| `objective.py` | امتیاز عینی جنسیت/سن/منطقه (۰ تا ۱۰۰) — معیارهای غیرقابل‌محاسبه حذف می‌شوند، نه صفر فرض می‌شوند |
| `specialization.py` | امتیاز تخصص (تطابق شرایط جسمانی سالمند با `accepted_physical_conditions` مراقب) و ساعات کاری (تطابق `needed_shifts` بیمار با `available_shifts` مراقب) — **فقط برای تای‌بریکر رتبه‌بندی**، نه بخشی از `objective_fit_score` یا معیارهای AHP/TOPSIS |
| (در `apps/care/trait_matching.py`) | پروفایل صفت‌محور، فرمول‌های Similarity/Adaptability، امتیاز هر بُعد، شاخص انعطاف‌پذیری فرهنگی (CFI) |
| `mcdm.py` (+ `ahp.py`, `topsis.py`) | رتبه‌بندی نهایی AHP-weighted TOPSIS روی ۶ معیار — وزن‌ها به‌طور خودکار بین معیارهای موجود بازتوزیع می‌شوند اگر معیاری برای همه نامزدها ناموجود باشد؛ شامل تای‌بریکر (`_ranking_sort_key`) برای امتیازهای مساوی |
| `explanations.py` | تبدیل اعداد خام به خلاصه قابل‌فهم («پیشنهاد بسیار قوی»)، سطح اطمینان (`high`/`medium`/`low`)، نقاط قوت/ضعف |
| `engine.py` | نقطه ورود عمومی (`suggest_caregivers_for_patient`) — هر مرحله بالا را به‌ترتیب اجرا می‌کند |

### تای‌بریکر رتبه‌بندی — رفع‌شده (۲۰۲۶-۰۸-۲۳)

طبق سند طراحی تطبیق (SAS-4)، وقتی دو مراقب امتیاز نهایی (`mcdm_score`) یکسان دارند، شش معیار
برای شکست تساوی مشخص شده: تخصص، ساعات کاری، فاصله جغرافیایی، زبان/گویش، سازگاری فرهنگی، سبک
زندگی. تا پیش از این تاریخ، فقط سه معیار آخر پیاده‌سازی شده بود چون داده ساختاریافته‌ای برای
«تخصص» و «ساعات کاری» در پرسشنامه یا پروفایل بیمار وجود نداشت.

اکنون دو فیلد جدید به `PatientProfile` اضافه شده (`physical_condition`، `needed_shifts`)، دقیقاً
هم‌ارز با فیلدهای موجود مراقب (`accepted_physical_conditions`، `available_shifts`) — و
`specialization.py` این دو را مقایسه می‌کند. زبان/گویش عمداً یک کلید جدا در تای‌بریکر نشده، چون
از قبل یکی از چهار ویژگی داخل بُعد `cultural_flexibility` است (`language_dialect_flexibility`) و
مقایسه مجزای دوباره، همان سیگنال را دوبار حساب می‌کرد.

**نکته طراحی**: این دو امتیاز فقط در تای‌بریکر استفاده می‌شوند، نه در `objective_fit_score` یا
معیارهای AHP/TOPSIS — چون افزودن آن‌ها به عنوان معیار رسمی AHP نیازمند بازتعریف کل ماتریس مقایسه
دوبه‌دو و تغییر ساختار `objective_criterion_scores` در فرانت‌اند بود؛ فراتر از دامنه همین رفع نقص.



### فرمول Adaptability — نکته مهم

`apps/care/trait_matching.py::adaptability_score` از فرمول **شکاف (gap)**
استفاده می‌کند، نه ضرب ساده:

```python
gap = max(0, patient_sensitivity - caregiver_flexibility)
score = max(0, 100 - gap)
```

این فرمول دو بار در طول توسعه این پروژه با فرمول ضربی ساده‌تر
(`sensitivity × flexibility / 100`) جایگزین شد و هر دو بار برگردانده شد —
چون فرمول ضربی، مراقبی که انعطاف‌پذیری‌اش از نیاز بیمار **بیشتر** است را
اشتباهاً جریمه می‌کند (مثلاً حساسیت=۵۰، انعطاف=۱۰۰ در فرمول ضربی فقط ۵۰
می‌شود، در حالی که باید ۱۰۰ باشد — نیازی کاملاً پوشش داده شده است). اگر در
آینده این فرمول دوباره تغییر کرد، این دلیل را در نظر بگیرید.

### رفع‌شده: `match_confidence` می‌تواند با خلاصه توضیح ناهم‌خوان باشد

`calculate_confidence` در `explanations.py` سطح اطمینان را **کاملاً بر
اساس وجود `objective_fit_score`** تعیین می‌کرد؛ اگر این امتیاز به هر
دلیلی محاسبه نشده بود (مثلاً هیچ ترجیح جنسیت/سن/منطقه‌ای برای مراقب ثبت
نشده)، اطمینان همیشه `low` می‌شد — حتی اگر تناسب روان‌سنجی (`trait_match
_score`) کامل و عالی باشد و `explanation.summary` بگوید «پیشنهاد بسیار
قوی». این یک تناقض واقعی و مشاهده‌شده بود (نه فرضی) که در تست‌های زنده
این پروژه تکرار شد.

با بررسی مجدد (۲۰۲۶-۰۸-۲۳) مشخص شد این یک باگ واقعی است، نه فقط یک سؤال
فلسفی محصول که نیاز به تصمیم‌گیری جداگانه داشته باشد: منطق تابع خودش
می‌گوید اطمینان باید «کامل بودن اطلاعات» را نشان دهد، اما فقط برای حالت
«فقط تناسب عینی موجود است» بررسی متقارن داشت و برای حالت متقارن «فقط
تناسب روان‌سنجی موجود است» هیچ بررسی‌ای نداشت — این یک سهل‌انگاری در کد
بود، نه انتخاب آگاهانه‌ای که منتظر ورودی محصول باشد. رفع شد: اکنون هر دو
حالت (فقط عینی، فقط روان‌سنجی) به‌طور یکسان `medium` محسوب می‌شوند. تست
regression مخصوص این حالت اضافه شد
(`test_trait_only_match_is_medium_not_low` در
`test_matching_explanations.py`).


## سیگنال‌های محاسبه‌شده

### امتیاز تناسب عینی (`objective_fit_score`، ۰ تا ۱۰۰)

| معیار | وزن | منطق |
|---|---|---|
| جنسیت | ۴۰٪ | ترجیح جنسیتی مراقب (`accepted_gender`) در برابر جنسیت بیمار — **الان هم فیلتر سخت در waterfall، هم امتیاز نرم اینجا** |
| بازه سنی | ۳۰٪ | بازه‌های سنی قابل‌قبول مراقب (`accepted_age_ranges`) در برابر سن محاسبه‌شده بیمار |
| منطقه جغرافیایی | ۳۰٪ | مناطق خدماتی ثبت‌شده مراقب در برابر استان/شهر بیمار (۱۰۰=شهر دقیق، ۶۰=فقط استان) |

معیار ناموجود به‌کلی حذف می‌شود (نه صفر) و وزن باقی‌مانده بازتوزیع می‌شود.
`objective_reasons` برای **هر سه معیار** توضیح می‌دهد — چه معیار محاسبه
شده باشد (مطابق/نامطابق) چه نشده باشد (چرا). کد: `apps/care/matching/
objective.py::calculate_objective_score`.

### امتیاز کاربران (`avg_rating`، میانگین ۱ تا ۵)

میانگین نظرات ثبت‌شده برای هر تخصیص واقعی. `null` وقتی هنوز نظری ثبت
نشده (نه صفر) — مراقب تازه‌کار نباید به‌خاطر نبود سابقه امتیاز پایین بگیرد.

### امتیاز انعطاف‌پذیری مستقل مراقب (`flexibility_score`، `flexibility_sections`)

از ۱۶ سؤال پرسشنامه مراقب محاسبه می‌شود، **مستقل از بیمار خاص** — حتی اگر
بیمار پرسشنامه‌ای پر نکرده باشد، این عدد در دسترس است. جدا از
`trait_match_score` زیر، که نیاز به هر دو پرسشنامه دارد.

### تناسب روان‌سنجی (`trait_match_score`، `trait_dimension_scores`)

نیازمند تکمیل **هر دو** پرسشنامه؛ در غیر این صورت `None` (نه صفر).
`trait_dimension_scores` از کلیدهای انگلیسی enum استفاده می‌کند
(`cultural_ritual`, ...)، نه برچسب فارسی مستقیم — برای برچسب نمایشی از
`trait_dimension_labels` همراه آن استفاده کنید.

### شاخص انعطاف‌پذیری فرهنگی مراقب (`caregiver_cfi`)

میانگین ۶ سؤال ویژه پرسشنامه مراقب (Q4، Q8، Q13، Q14، Q15، Q16).

### امتیاز نهایی رتبه‌بندی (`mcdm_score`) — AHP + TOPSIS

رتبه‌بندی نهایی همه نامزدها روی ۶ معیار (`objective_fit`،
`cultural_ritual`، `values_professional`، `lifestyle`،
`cultural_flexibility`، `reputation`) با وزن‌های AHP:

```python
# apps/care/matching/mcdm.py
AHP_PAIRWISE_MATRIX = [
    # objective, cultural, values, lifestyle, flexibility, reputation
    [1,   2,   2,   2,   2,   2],
    [1/2, 1,   2,   2,   1,   1],
    [1/2, 1/2, 1,   2,   1,   1],
    [1/2, 1/2, 1/2, 1,   1,   1],
    [1/2, 1,   1,   1,   1,   1],
    [1/2, 1,   1,   1,   1,   1],
]
```

این ماتریس **پیکربندی محصول است، نه نتیجه علمی اعتبارسنجی‌شده** — دقیقاً
طبق سند اصلی این سیستم، قرار است بعداً با نظر خبرگان/AHP واقعی و داده
کاربران واقعی بازبینی شود. اگر معیاری برای **همه** نامزدها ناموجود باشد
(مثلاً هیچ‌کدام امتیاز تناسب عینی ندارند)، آن معیار کامل حذف می‌شود و
وزن‌های باقی‌مانده بازتوزیع می‌شوند — نه اینکه با یک عدد جعلی پر شود.
نسبت سازگاری (`ahp_consistency_ratio`) کمتر از ۰.۱۰ نشان می‌دهد قضاوت‌های
زوجی این ماتریس منطقاً ناسازگار نیستند.

### توضیح خودکار (`explanation`، `match_confidence`)

`explanations.py` امتیازهای خام را به یک خلاصه قابل‌فهم («پیشنهاد بسیار
قوی» تا «تناسب پایین»)، فهرست نقاط قوت/ضعف، و سطح اطمینان
(`high`/`medium`/`low`) تبدیل می‌کند — طراحی‌شده برای اینکه ناظر بدون
نیاز به تفسیر اعداد خام، سریع بفهمد این پیشنهاد چقدر قابل‌اتکاست.

### سیگنال اضافی: ظرفیت فعلی (`active_patient_count`)

تعداد بیمارانی که مراقب **در حال حاضر** به‌طور فعال به آنها تخصیص دارد.
صرفاً اطلاعاتی — نه فیلتر می‌کند، نه امتیاز را کم می‌کند.

---


## پرسشنامه سازگاری بیمار (۱۲ سؤال، ۷ محور)

قبلاً در `apps.families.PatientCompatibilityQuestionnaire` ساخته شده بود.
هر خانواده یا خود بیمار (در صورت داشتن حساب) آن را پر می‌کند.

| فیلد | محور | نوع مقیاس |
|---|---|---|
| `religious_beliefs_priority` | محور عقیدتی-مناسکی | AGREEMENT_SCALE |
| `new_treatment_openness` | محور عقیدتی-مناسکی | INTENSITY_SCALE |
| `caregiver_as_family_member` | محور جمع‌گرایی | YES_NO_PARTIAL |
| `respectful_disagreement_acceptance` | محور جمع‌گرایی | ACCEPTANCE_SCALE |
| `privacy_comfort_with_caregiver` | محور حریم خصوصی | YES_NO_PARTIAL |
| `noise_smell_sensitivity` | محور سبک زندگی | INTENSITY_SCALE |
| `meal_time_strictness` | محور سبک زندگی | TIMING_STRICTNESS_SCALE |
| `special_diet_preference` | محور سبک زندگی | YES_NO_PARTIAL |
| `medication_timing_priority` | محور جهت‌گیری زمانی | TIMING_STRICTNESS_SCALE |
| `accent_customs_annoyance` | محور تفاوت فرهنگی/نسلی | DISTURBANCE_SCALE |
| `cultural_respect_expectation` | محور تفاوت فرهنگی/نسلی | YES_NO_PARTIAL |
| `willingness_to_express_opinion` | محور انعطاف‌پذیری کلی | EXPRESSION_WILLINGNESS_SCALE |

مقیاس‌های کامل در `frontend/family-panel/lib/constants.ts` تعریف شده‌اند
(AGREEMENT_SCALE، INTENSITY_SCALE، YES_NO_PARTIAL، ACCEPTANCE_SCALE،
DISTURBANCE_SCALE، TIMING_STRICTNESS_SCALE، EXPRESSION_WILLINGNESS_SCALE)
و به‌طور یکسان در `frontend/patient-panel/lib/constants.ts` تکرار شده‌اند.

✅ **رفع‌شده (۲۰۲۶-۰۸-۲۳)**: سه فیلدی که از همان روزهای اول پروژه روی
مقیاس عمومی placeholder (`INTENSITY_SCALE`) باقی مانده بودند —
`meal_time_strictness`، `medication_timing_priority`، و
`willingness_to_express_opinion` — اکنون گزینه‌های واقعی و مخصوص به خود
را دارند:
- `meal_time_strictness` و `medication_timing_priority` هر دو از
  `TIMING_STRICTNESS_SCALE` جدید استفاده می‌کنند (بسیار مهم/نسبتاً
  مهم/چندان مهم نیست/بدون اهمیت) — هر دو سؤال اساساً یک چیز را می‌پرسند:
  برنامه روزانه بیمار چقدر باید دقیق رعایت شود.
- `willingness_to_express_opinion` از `EXPRESSION_WILLINGNESS_SCALE`
  جدید استفاده می‌کند (همیشه/بیشتر مواقع/به‌ندرت/تمایلی ندارد) — میزان
  واقعی ابراز نظر بیمار، نه شدت آن.

مقادیر امتیازدهی در `QuestionTraitMapping` (جدول نگاشت سؤال↔ویژگی، از
طریق `seed_trait_mappings.py`) بدون تغییر باقی ماندند و فقط روی کلیدهای
گزینه جدید منتقل شدند — یعنی آنچه قبلاً `"very_high": 85` بود اکنون
`"very_strict": 85` است؛ فقط متن و کلید گزینه‌ها placeholder بودند، نه
خودِ امتیازدهی کالیبره‌شده.

---

## پرسشنامه سازگاری مراقب (۱۶ سؤال، ۴ بخش) — منبع اصلی: سند ارسالی شما

مدل: `apps.caregivers.CaregiverCompatibilityQuestionnaire`. هر سؤال دقیقاً
همان ساختار را دارد: گزینه **الف** همیشه منعطف‌ترین/سازگارترین پاسخ است، گزینه
**د** همیشه پاسخی است که خواهان تشابه یا دارای محکم‌ترین مرز حرفه‌ای است.
همین یکدستی است که امتیازدهی مشترک (الف=۴ تا د=۱) را برای هر ۱۶ سؤال
قابل‌توجیه می‌کند.

### بخش اول: هم‌راستایی عقیدتی و مناسکی

**۱. `religious_belief_accommodation`** — اگر سالمندی که از او مراقبت می‌کنید
باور مذهبی یا اعتقادی متفاوتی با شما داشته باشد و بخواهد مراقبت‌های روزمره
با رعایت آن باورها انجام شود، واکنش طبیعی شما چیست؟
- الف) کاملاً مطابق باورهای او عمل می‌کنم، تا زمانی که با وظایف حرفه‌ای تعارض جدی نداشته باشد.
- ب) به باور او احترام می‌گذارم، اما مراقبت را بر اساس اصول حرفه‌ای خودم انجام می‌دهم.
- ج) تلاش می‌کنم بین باور او و الزامات مراقبتی راه‌حل میانه پیدا کنم.
- د) ترجیح می‌دهم در محیطی کار کنم که باورهای مذهبی و اعتقادی من و سالمند تا حد زیادی مشابه باشد.

**۲. `physical_contact_sensitivity_adaptation`** — اگر سالمند نسبت به تماس
بدنی یا انجام مراقبت‌های شخصی توسط فردی با جنسیت متفاوت حساسیت فرهنگی داشته
باشد، تا چه اندازه حاضر هستید شیوه مراقبت خود را با این ترجیح تطبیق دهید؟
- الف) کاملاً آماده‌ام روش مراقبت را متناسب با این حساسیت تغییر دهم.
- ب) تا جایی که کیفیت و ایمنی مراقبت حفظ شود، خودم را تطبیق می‌دهم.
- ج) ترجیح می‌دهم مراقبت را مطابق روش حرفه‌ای استاندارد خودم انجام دهم.
- د) ترجیح می‌دهم چنین مراقبت‌هایی توسط فرد هم‌جنس انجام شود.

**۳. `prayer_time_scheduling_flexibility`** — اگر زمان انجام یک وظیفه
مراقبتی با زمان نماز، دعا یا مناسک مذهبی سالمند تداخل داشته باشد، معمولاً چه
رویکردی دارید؟
- الف) برنامه مراقبتی را تا حد امکان با زمان مناسک او هماهنگ می‌کنم.
- ب) تلاش می‌کنم بین زمان مراقبت و مناسک مذهبی تعادل ایجاد کنم.
- ج) وظیفه مراقبتی را در زمان تعیین‌شده انجام می‌دهم و انتظار دارم سالمند برنامه مذهبی خود را با آن هماهنگ کند.
- د) ترجیح می‌دهم سالمندی را مراقبت کنم که سبک مذهبی مشابهی با من داشته باشد.

**۴. `traditional_belief_acceptance`** — اگر سالمند باور سنتی یا فرهنگی‌ای
داشته باشد که از نظر شما علمی نیست، اما برای او اهمیت زیادی دارد، چقدر
می‌توانید آن باور را بدون تمسخر یا مخالفت مستقیم بپذیرید؟
- الف) به‌راحتی می‌توانم آن را بپذیرم، حتی اگر شخصاً به آن اعتقاد نداشته باشم.
- ب) می‌پذیرم، مگر اینکه مستقیماً سلامت سالمند را به خطر بیندازد.
- ج) معمولاً تلاش می‌کنم با توضیح علمی نظر او را تغییر دهم.
- د) برایم دشوار است که باورهای مخالف دیدگاه خودم را در محیط کار بپذیرم.

### بخش دوم: هم‌راستایی ارزش‌های بنیادین و مرزهای حرفه‌ای

**۵. `family_event_participation`** — اگر خانواده سالمند شما را به یک
مناسبت خانوادگی مانند شام، جشن یا مهمانی دعوت کنند، در چه شرایطی حضور پیدا
کردن برای شما راحت است؟
- الف) با کمال میل شرکت می‌کنم و ارتباط خانوادگی را بخشی از مراقبت می‌دانم.
- ب) برای مدت کوتاهی شرکت می‌کنم، اما مرز حرفه‌ای خود را حفظ می‌کنم.
- ج) ترجیح می‌دهم خارج از محیط کاری در برنامه‌های خانوادگی شرکت نکنم.
- د) فقط در صورتی شرکت می‌کنم که حضور من از نظر حرفه‌ای و کاری توجیه داشته باشد.

**۶. `false_accusation_reaction`** — اگر سالمند به دلیل فراموشی یا بیماری
شما را به اشتباه متهم کند که وسیله‌ای را برداشته‌اید، واکنش طبیعی شما چیست؟
- الف) ناراحت نمی‌شوم و ابتدا به او کمک می‌کنم وسیله را پیدا کند.
- ب) آرامش خودم را حفظ می‌کنم و در عین حال مرز حرفه‌ای خود را توضیح می‌دهم.
- ج) ترجیح می‌دهم فوراً به خانواده اطلاع دهم تا موضوع را مدیریت کنند.
- د) چنین اتهامی برایم بسیار آزاردهنده است و احتمالاً ادامه کار برایم دشوار خواهد شد.

**۷. `confidentiality_commitment`** — اگر فردی خارج از خانواده درباره
وضعیت جسمی، روانی یا خانوادگی سالمندی که از او مراقبت می‌کنید سؤال کند،
چقدر به حفظ محرمانگی اطلاعات اهمیت می‌دهید؟
- الف) اطلاعات را کاملاً محرمانه نگه می‌دارم.
- ب) فقط اطلاعاتی را که خود سالمند یا خانواده اجازه داده‌اند مطرح می‌کنم.
- ج) اگر شخص سؤال‌کننده از نزدیکان سالمند باشد، ممکن است اطلاعات محدودی ارائه کنم.
- د) اگر فکر کنم اطلاعات می‌تواند به سالمند کمک کند، ممکن است موضوع را با فرد قابل‌اعتماد در میان بگذارم.

**۸. `gender_based_task_flexibility`** — اگر خانواده سالمند از شما بخواهند
برخی وظایف را صرفاً به دلیل زن یا مرد بودن شما انجام ندهید، واکنش طبیعی شما
چیست؟
- الف) تا زمانی که با اصول حرفه‌ای و ایمنی تعارض نداشته باشد، با فرهنگ خانواده هماهنگ می‌شوم.
- ب) درباره دلیل این ترجیح گفت‌وگو می‌کنم و تلاش می‌کنم راه‌حل مناسبی پیدا کنیم.
- ج) ترجیح می‌دهم وظایف مراقبتی بر اساس تخصص باشد، نه جنسیت.
- د) چنین تفاوتی برایم قابل‌قبول نیست و ترجیح می‌دهم در چنین محیطی کار نکنم.

### بخش سوم: هم‌راستایی سبک زندگی و محیط کاری

**۹. `home_environment_adaptability`** — اگر محیط خانه سالمند از نظر بو،
نظافت یا شرایط فیزیکی با استانداردهای شخصی شما متفاوت باشد، میزان سازگاری
شما چگونه است؟
- الف) معمولاً به‌راحتی با محیط سازگار می‌شوم.
- ب) تا زمانی که شرایط برای سلامت و ایمنی قابل‌قبول باشد، مشکلی ندارم.
- ج) نیاز دارم حداقل استانداردهای بهداشتی محیط رعایت شود.
- د) محیط نامناسب به‌شدت روی رضایت و عملکرد من تأثیر می‌گذارد.

**۱۰. `schedule_flexibility_for_family_events`** — اگر برنامه مراقبتی
سالمند با یک رویداد خانوادگی مهم یا حضور مهمانان تداخل پیدا کند، کدام گزینه
بیشتر به سبک شما نزدیک است؟
- الف) برنامه مراقبتی را حفظ می‌کنم و از خانواده می‌خواهم شرایط را با آن هماهنگ کنند.
- ب) برنامه را تا حد امکان با شرایط خانواده تطبیق می‌دهم.
- ج) تلاش می‌کنم فعالیت مراقبتی را با حضور خانواده ترکیب کنم.
- د) ترجیح می‌دهم برنامه مراقبتی دقیق و بدون تغییر اجرا شود.

> ⚠️ توجه: در سند اصلی، ترتیب معنایی این گزینه‌ها با الگوی «الف=منعطف‌ترین»
> کمی متفاوت است (الف در اینجا سخت‌گیرانه‌تر از ب و ج به نظر می‌رسد). این
> سؤال دقیقاً به همان صورتی که ارسال شد پیاده‌سازی و ذخیره شده، اما ارزش
> بازبینی محتوایی دارد.

**۱۱. `traditional_food_treatment_openness`** — اگر سالمند به غذاها،
نوشیدنی‌ها یا روش‌های درمانی سنتی علاقه داشته باشد که با سبک زندگی شما
متفاوت است، چه میزان آمادگی برای سازگاری دارید؟
- الف) کاملاً می‌توانم با این تفاوت کنار بیایم.
- ب) تا زمانی که با دستور پزشکی تعارض نداشته باشد، آن را می‌پذیرم.
- ج) ترجیح می‌دهم سالمند از روش‌های مورد تأیید پزشکی استفاده کند.
- د) تفاوت زیاد در سبک تغذیه و درمان سنتی برایم دشوار است.

**۱۲. `personal_conversation_patience`** — اگر سالمند هنگام انجام یک
وظیفه مراقبتی شروع به صحبت درباره خاطرات، احساسات یا مسائل شخصی خود کند،
شما معمولاً چه می‌کنید؟
- الف) ترجیح می‌دهم با حوصله گوش کنم، حتی اگر کمی از برنامه عقب بیفتیم.
- ب) هم‌زمان به صحبت‌های او گوش می‌دهم و برنامه مراقبتی را نیز مدیریت می‌کنم.
- ج) ابتدا وظیفه مراقبتی را انجام می‌دهم و سپس فرصت کافی برای صحبت کردن اختصاص می‌دهم.
- د) ترجیح می‌دهم گفت‌وگوهای شخصی تا حد امکان از وظایف مراقبتی جدا باشند.

**۱۳. `home_organization_adaptability`** — اگر خانه سالمند از نظر نظم،
چیدمان وسایل و سبک زندگی با سلیقه شخصی شما تفاوت زیادی داشته باشد، چقدر
می‌توانید خود را تطبیق دهید؟
- الف) به‌راحتی با سبک زندگی او کنار می‌آیم و فقط موارد خطرناک را تغییر می‌دهم.
- ب) تا زمانی که ایمنی رعایت شود، ترجیح می‌دهم چیدمان خانه را تغییر ندهم.
- ج) ترجیح می‌دهم محیط کار مرتب و منظم باشد و درباره آن با خانواده صحبت می‌کنم.
- د) بی‌نظمی محیط برایم بسیار آزاردهنده است و بر انتخاب محل کارم تأثیر می‌گذارد.

### بخش چهارم: انعطاف‌پذیری فرهنگی و هوش فرهنگی

**۱۴. `cultural_expression_tolerance`** — اگر سالمند از واژه‌ها، شوخی‌ها یا
دیدگاه‌هایی استفاده کند که با ارزش‌های شخصی شما متفاوت یا حتی برایتان
ناخوشایند است، چه میزان می‌توانید بدون ایجاد تنش به مراقبت ادامه دهید؟
- الف) معمولاً آن را نادیده می‌گیرم و تفاوت نسلی و فرهنگی را در نظر می‌گیرم.
- ب) اگر لازم باشد، محترمانه درباره آن صحبت می‌کنم.
- ج) چنین موضوعاتی برایم آزاردهنده است و ترجیح می‌دهم خانواده در جریان باشند.
- د) اگر این رفتارها ادامه‌دار باشد، ترجیح می‌دهم همکاری را ادامه ندهم.

**۱۵. `unfamiliar_custom_acceptance`** — اگر خانواده سالمند رسم یا آیینی
داشته باشند که از نظر شما ناآشناست، مانند اسپند دود کردن، نذر، دعا یا مراسم
خانوادگی، واکنش شما چیست؟
- الف) تا زمانی که خطری برای سلامت ایجاد نکند، کاملاً با آن کنار می‌آیم.
- ب) به رسم آن‌ها احترام می‌گذارم، حتی اگر شخصاً به آن اعتقاد نداشته باشم.
- ج) اگر آن رسم برای سلامت مشکل ایجاد کند، درباره آن با خانواده مذاکره می‌کنم.
- د) ترجیح می‌دهم در محیطی کار کنم که سبک فرهنگی آن به ارزش‌های من نزدیک باشد.

**۱۶. `dialect_communication_effort`** — اگر سالمند لهجه، گویش یا
اصطلاحات محلی متفاوتی داشته باشد و گاهی در برقراری ارتباط با او دچار مشکل
شوید، معمولاً چه می‌کنید؟
- الف) تلاش می‌کنم اصطلاحات و شیوه صحبت کردن او را یاد بگیرم.
- ب) از او می‌خواهم صحبتش را تکرار کند یا با زبان ساده‌تر توضیح دهد.
- ج) از خانواده برای درک بهتر منظور او کمک می‌گیرم.
- د) ترجیح می‌دهم با سالمندی کار کنم که از نظر زبانی و گویشی با من اشتراک بیشتری داشته باشد.

### فرمول امتیازدهی

هر پاسخ: الف=۴، ب=۳، ج=۲، د=۱. امتیاز هر بخش:

```
امتیاز بخش = (میانگین امتیاز سؤالات آن بخش − ۱) ÷ ۳ × ۱۰۰
```

(یعنی همه پاسخ‌ها «د» = ۰٪، همه پاسخ‌ها «الف» = ۱۰۰٪). امتیاز کلی همین
فرمول روی هر ۱۶ سؤال. کد: `CaregiverCompatibilityQuestionnaire.section_scores()`
و `.overall_flexibility_score()` در `apps/caregivers/models.py`.

جای پر شدن: صفحه بررسی مراقب (`caregivers/review`) یا مرحله ۶ (اختیاری) از
ویزارد افزودن مراقب جدید (`caregivers/new`) در supervisor-panel.

---

## نگاشت محور بیمار ↔ بخش مراقب

نگاشت بر اساس **محتوا و موضوع**، نه فرمول عددی — در
`frontend/supervisor-panel/lib/constants.ts::PATIENT_AXIS_TO_CAREGIVER_SECTION`:

| محور بیمار | بخش مراقب |
|---|---|
| محور عقیدتی-مناسکی | عقیدتی و مناسکی |
| محور جمع‌گرایی | ارزش‌های بنیادین و مرزهای حرفه‌ای |
| محور حریم خصوصی | ارزش‌های بنیادین و مرزهای حرفه‌ای |
| محور سبک زندگی | سبک زندگی و محیط کاری |
| محور تفاوت فرهنگی/نسلی | انعطاف‌پذیری فرهنگی |

**عمداً بدون نگاشت**: محور جهت‌گیری زمانی، محور انعطاف‌پذیری کلی — هیچ
بخش مراقب به‌اندازه کافی نزدیک به این دو محور نیست که نمایش کنار هم گمراه‌کننده
نباشد.

## مرجع کامل API

### مراقب‌محور (`apps.caregivers`)

| متد | مسیر | دسترسی | توضیح |
|---|---|---|---|
| GET/PUT | `/api/supervisor/caregivers/<id>/compatibility-questionnaire/` | ناظر | پرسشنامه ۱۶سؤالی مراقب |

### بیمارمحور (`apps.families`)

| متد | مسیر | دسترسی | توضیح |
|---|---|---|---|
| GET/PUT | `/api/patients/<id>/questionnaire/` | خانواده (ویرایش نیاز به `full_access`) | پرسشنامه ۱۲سؤالی |
| GET/PUT | `/api/patients/me/questionnaire/` | بیمار (حساب خودش) | همان، از دید بیمار |

### تطابق و تخصیص (`apps.care`)

| متد | مسیر | دسترسی | توضیح |
|---|---|---|---|
| GET | `/api/care/suggest-caregivers/?patient_code=` | ناظر | لیست رتبه‌بندی‌شده مراقبان پیشنهادی |
| GET | `/api/care/patient-questionnaire/?patient_code=` | ناظر | پاسخ‌های خام پرسشنامه بیمار (برای مقایسه) |
| GET/POST | `/api/care/assignments/?caregiver_user_id=` | ناظر | لیست/ایجاد تخصیص |
| POST | `/api/care/assignments/<id>/end/` | ناظر | پایان تخصیص |
| POST | `/api/care/assignments/<id>/review/` | خانواده/بیمار (با دسترسی به بیمار) | ثبت/به‌روزرسانی نظر و امتیاز |
| GET | `/api/care/me/patients/` | مراقب | بیماران تخصیص‌یافته فعلی |
| GET/POST | `/api/care/me/log-entries/` | مراقب | گزارش‌های مراقبتی |
| GET | `/api/care/patients/<id>/team/` | خانواده/بیمار | تیم مراقبت فعلی + امتیاز |
| GET | `/api/care/patients/<id>/timeline/` | خانواده/بیمار | تاریخچه گزارش‌های مراقبتی |

### شکل پاسخ `suggest-caregivers`

```json
{
  "patient_name": "...",
  "patient_gender": "female",
  "suggestions": [
    {
      "caregiver_user_id": 12,
      "caregiver_name": "...",
      "caregiver_gender": "female",
      "waterfall_reasons": ["..."],
      "objective_fit_score": 88,
      "objective_criterion_scores": {"gender": 100, "age": 100, "location": 60},
      "objective_reasons": ["..."],
      "score": 88,
      "physical_condition_match_score": 100,
      "shift_availability_score": 100,
      "trait_match_available": true,
      "trait_match_score": 91,
      "trait_dimension_scores": {"cultural_ritual": 83, "...": 100},
      "trait_dimension_labels": {"cultural_ritual": "عقیدتی و مناسکی", "...": "..."},
      "caregiver_cfi": 89,
      "flexibility_score": 90,
      "flexibility_sections": {"عقیدتی و مناسکی": 83, "...": 100},
      "avg_rating": 4.5,
      "review_count": 3,
      "active_patient_count": 2,
      "mcdm_score": 92.96,
      "mcdm_method": "AHP-TOPSIS",
      "ahp_weights": {"objective_fit": 0.22, "...": "..."},
      "ahp_consistency_ratio": 0.017,
      "match_confidence": "high",
      "explanation": {
        "summary": "پیشنهاد بسیار قوی",
        "confidence": "high",
        "strengths": ["..."],
        "weaknesses": []
      }
    }
  ]
}
```

---

## کجا در کد پیدا کنید

- `backend/apps/care/matching/engine.py` — نقطه ورود اصلی (`suggest_caregivers_for_patient`)
- `backend/apps/care/matching/waterfall.py` — فیلتر سخت (تأیید، جنسیت، منطقه)
- `backend/apps/care/matching/objective.py` — امتیاز عینی جنسیت/سن/منطقه
- `backend/apps/care/matching/specialization.py` — امتیاز تخصص و ساعات کاری (فقط تای‌بریکر)
- `backend/apps/care/matching/mcdm.py` + `ahp.py` + `topsis.py` — رتبه‌بندی نهایی
- `backend/apps/care/matching/explanations.py` — خلاصه و سطح اطمینان
- `backend/apps/care/trait_matching.py` — پروفایل صفت‌محور، Similarity/Adaptability، CFI
- `backend/apps/care/models.py` — `CaregiverAssignment`، `CaregiverReview`، `QuestionTraitMapping`
- `backend/apps/caregivers/models.py` — `CaregiverCompatibilityQuestionnaire`
- `backend/apps/families/models.py` — `PatientCompatibilityQuestionnaire`، `PatientProfile.physical_condition`/`needed_shifts`
- `backend/tests/integration/test_care.py` — تست‌های end-to-end تطابق، تخصیص، نظرات، ظرفیت
- `backend/tests/integration/test_matching_*.py` — تست‌های واحد هر بخش از پکیج matching
- `backend/tests/integration/test_trait_matching.py`
- `backend/tests/integration/test_caregiver_compatibility_questionnaire.py`
- `frontend/supervisor-panel/app/match/page.tsx` — صفحه جست‌وجو، پیشنهاد، توضیح خودکار
- `frontend/supervisor-panel/lib/compatibility-questionnaire.ts` — متن کامل ۱۶ سؤال
- `frontend/supervisor-panel/lib/constants.ts` — نگاشت محور↔بخش، برچسب‌های پرسشنامه بیمار

## آنچه هنوز باز است

- امتیاز واحد ترکیبی بین دو پرسشنامه: عمداً ساخته نشده (دلیل بالا)
- ~~ناهم‌خوانی شناخته‌شده بین `match_confidence` و `explanation.summary` در برخی موارد~~ — رفع شد (۲۰۲۶-۰۸-۲۳، بالا توضیح داده شده)
- وزن‌های AHP در `mcdm.py` هاردکد هستند، رابط کاربری برای تنظیم آن توسط ناظر وجود ندارد
- ~~۳ فیلد پرسشنامه بیمار هنوز روی مقیاس placeholder‌اند~~ — رفع شد (۲۰۲۶-۰۸-۲۳، بالا در بخش «پرسشنامه سازگاری بیمار» توضیح داده شده)
- خانواده/بیمار نمی‌توانند خودشان مراقب پیشنهادی را مرور کنند — فقط ناظر
- تست واقعی در مرورگر هرگز انجام نشده — تمام تأییدها build موفق + آزمایش زنده API بوده‌اند
